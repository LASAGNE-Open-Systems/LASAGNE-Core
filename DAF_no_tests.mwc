workspace {
  cmdline += -include $DAF_ROOT/MPC/config \
             -feature_file $DAF_ROOT/MPC/config/TAF.features
  daf
  GCRoute
  LDBC/LDBC.mwc
  TTY_Asynch

  exclude {
    ext
    mvn
    tests
  }
}

